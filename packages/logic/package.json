{
  "name": "@tt2-build-wizard/logic",
  "version": "1.0.0",
  "main": "dist/optimizer.js",
  "module": "dist/optimizer.js",
  "types": "dist/optimizer.d.ts",
  "type": "module",
  "scripts": {
    "build": "bun build ./src/optimizer.ts --outdir ./dist && tsc --emitDeclarationOnly --declaration --project tsconfig.json"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
